--- a/CMakeLists.txt
+++ b/CMakeLists.txt
@@ -103,9 +103,8 @@
     gflags
     glog
     stdc++fs
-    tinyxml
 )
-set(frt_public_link_libraries Threads::Threads)
+set(frt_public_link_libraries Threads::Threads TinyXML::TinyXML)

 add_library(frt_static STATIC)
 add_library(frt ALIAS frt_static)
@@ -201,6 +200,12 @@
   DESTINATION ${ConfigPackageLocation}
   COMPONENT Devel
 )
+install(
+  FILES cmake/FindTinyXML.cmake
+  RENAME TinyXMLConfig.cmake
+  DESTINATION ${ConfigPackageLocation}
+  COMPONENT Devel
+)

 set(CPACK_PACKAGE_NAME libfrt-dev)
 set(CPACK_PACKAGE_VERSION_MAJOR 0)
@@ -259,6 +264,8 @@
 )
 include(CPack)

+include(CTest)
+if(BUILD_TESTING)
 enable_testing()

 include(GoogleTest)
@@ -269,3 +276,4 @@

 add_subdirectory(tests/hbm)
 add_subdirectory(tests/xdma)
+endif(BUILD_TESTING)
